export const metadata = {
    title: '功能反馈',
    description: 'ProrisePromptMinder 功能反馈',
    alternates: { canonical: '/feedback' },
    robots: { index: true, follow: true },
};

export default function FeedbackLayout({ children }) {
    return children;
}

